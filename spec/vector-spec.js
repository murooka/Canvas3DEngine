describe("Vector(1, 1, 1)", function () {
    var vector;

    beforeEach(function () {
        vector = new Vector(1, 1, 1);
    });

    it("should be equal to Vector(1,1,1)", function () {
        
    });
});
